# Ubuntu / Docker 最终复验 · 2026-09-12

## 结论与代码版本

**Ubuntu 本地／私有部署的工程验收通过；正式发布仍为 NO-GO。** 前端、后端、
生产 Docker、Nginx 流式代理及 Chromium 三流程验收已实际执行。真实模型内容质量、
原生 Safari/iOS 和实体中文输入法尚未验收，不批准无鉴权公网／SaaS 暴露。

本报告更新[首轮验收记录](ACCEPTANCE-2026-09-12.md)中的测试数量、依赖和提交状态，
首轮缺陷台账继续保留。被验证代码为 `f724cd70e57b5f6fe1abddd86a13c81d3d3b5a5a`。
复验期间共享工作区出现并发写入及提交；没有重置或改写该提交，最终核对了
170 个源码／构建／验收文件的 SHA-256，均与测试快照一致。后续提交仅归档本报告，
不改变被验证代码。本次未推送，不将本地通过表述为远程 CI 已通过。

精简证据见[结果 JSON](acceptance/2026-09-12-recheck.json)及
[源码指纹](acceptance/2026-09-12-recheck.sha256)。浏览器最终轮次为北京时间
13:39:32–13:44:07；Python 3.12 后端最终检查完成于 13:49:10。

## 实际通过的检查

| 检查 | 结果 | 范围 |
|---|---|---|
| 前端干净安装／依赖审计 | 通过，0 漏洞 | npm ci，198 个审计包；包括开发依赖，仅代表当次 npm 审计结果 |
| 前端 Vitest | 136 / 136，20 文件 | Vitest 4.1.11 |
| 前端 ESLint／TypeScript／Vite | 全部通过 | 独立临时目录构建；JS 396.59 kB，gzip 116.06 kB |
| 后端 Python 3.12 | 189 / 189 | 180 应用测试 + 5 依赖锁测试 + 4 验收运行器安全测试；1 条上游弃用警告 |
| 后端依赖／静态检查 | 全部通过 | pip check、24 个生产包／33 个当前平台开发包闭包检查、Ruff 0.16.7 |
| 本机真实 HTTP | 67 / 67 | 独立 uvicorn + 本地模型协议桩 |
| Docker 后端直连 | 67 / 67 | 实际生产后端镜像 |
| Docker Nginx 代理 | 69 / 69 | 实际增量 SSE 到达及生产 HTML／JS 获取 |
| Chromium 交互 | 30 / 30 组 | 最终 Docker 前端 + 受控 API 响应 |
| Chromium → Docker 全链路 | 3 / 3 | 原创、公众号、长文改写；Nginx → FastAPI → 本地模型协议桩 |
| 部署契约／容器内部 | 全部通过 | 无真实 .env 的 Compose 插值、回环地址绑定、端口覆盖、项目隔离、3 服务健康、Nginx 配置 |

每套 HTTP 验收各产生 75 次合成协议桩调用；本轮真实模型调用为 **0**。
浏览器覆盖的草稿安全、候选、恢复、导出、布局和故障场景见首轮台账与结果 JSON。
390／860／1440／1920 宽度及浅深色结果不等价于原生设备验收。

环境：Ubuntu 22.04.3、Node 22.22.2、npm 10.9.7、测试 Python 3.12.13、
容器 Python 3.12.14、Docker 29.1.3。本次复验使用系统 Compose 2.40.3、
Buildx 0.30.1，与首轮记录的临时工具版本不同。浏览器为 Playwright 1.63.0 +
Chromium headless shell 153.0.8010.12。本机 HTTP 运行器使用现有 Python 3.14.5。

## 本次补充修复与回归

- 补齐空结果校验：改写标签分组的空白名称／空标签，以及原创生成、旧版标签重生成
  的空标签／空配图关键词，不再误报 `done`。新增 7 项先失败、修复后通过的回归测试。
- 固定模型默认列表单测的环境，避免宿主模型配置污染测试结果。
- 更新前端兼容范围内的开发依赖，当前 npm 审计归零；后端增加生产／开发哈希锁、
  闭包校验及测试，Docker／CI 按锁安装；Nginx 固定到版本及镜像摘要。
- Compose 配置检查增加清除 env_file 的临时覆盖，缺少真实 .env 时也能检查插值，
  不要求或读取真实模型凭据。
- 浏览器截图禁用有限动画，布局矩阵拍摄前回到页顶，消除动画中间帧和滚动截断。
  这是验收脚本稳定性修复，不是额外的产品 CSS 改动。
- CI 已配置依赖检查、生产 Docker、HTTP、Chromium mock／integration，以及按提交
  SHA 留存 14 天的验收产物；配置完成不代表这个提交已通过远程 CI。

## 证据边界

生产后端镜像实际执行了 `pip install --require-hashes --only-binary=:all:`，并通过
pip check；运行容器中的 23 个 Python 源文件与当前代码完全一致。最终 Docker
前端直接接受了浏览器及代理验收，镜像 ID 和构建资产摘要已归档。独立本机与
Docker 构建的资产摘要不同，本报告不声称构建产物逐字节可重复。

Python 3.12 最终测试环境复用了上述 Docker 中已按哈希安装的运行时包，并复制了
现有完整 Ruff 0.16.7 包及原始元数据；其他开发依赖按开发锁及哈希安装，最终
版本和闭包均通过检查。没有修改包元数据，但本次未重新验证 Ruff wheel 哈希，
因此不声称所有开发包都重新下载并逐一验哈希。完整干净安装仍由 CI 命令约束。

TestClient 在受限沙箱中启动挂起；在批准的沙箱外环境中 Python 3.12 和 3.14 均能
通过，不将此问题归因为 Python 3.14 不兼容。前端测试／lint／build 的原始输出
保存在工具运行记录中，没有额外独立 stdout 文件；摘要不虚构日志或精确结束时间。

## 清理与复跑

本轮独立项目 `opera-verify-oxusg4` 的 3 个容器、项目网络、2 个最终镜像标签、
初次构建的无用前端镜像，以及独立 Buildx builder 的容器、状态卷和缓存已移除。
23101／28080／28081／28180 端口已释放。

只清理本轮自建临时目录：独立 Python／Node 环境、包缓存、浏览器与配置、截图、
合成稿件／图片下载和构建副本。清理状态与精确路径见结果 JSON。保留必要的精简
证据和复跑脚本，未删除用户文件、现有开发环境、其他项目容器或共享基础镜像；
没有运行全局 prune。这些临时资源可重新生成，不保留原始大体积截图和日志。

按[验收说明](../tests/acceptance/README.md)可重新运行。README 中的工具版本属于
先前验证组合，本轮实际环境以本报告为准。核对本次被测文件指纹：

```bash
sha256sum -c docs/acceptance/2026-09-12-recheck.sha256
```

下一步仅剩需要真实条件的发布门禁：配置获准调用的真实模型并人工检查内容质量，
补齐原生 Safari/iOS／实体中文输入法，以及获准推送后确认远程 CI。本轮工程
验收与修复任务的完成不替代这些发布批准。
