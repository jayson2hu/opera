# Archive Notes

This folder contains historical handoff documents. They are retained for traceability, not as the current source of truth.

Use the current docs first:

- `../PROJECT.md`
- `../DEPLOYMENT.md`
- `../REVIEW.md`
- `../PLAN.md`

## File Status

- `handoff.md`: product handoff history. The top 2026-04-25 update is readable and useful; older sections contain mojibake and should be treated as historical context only.
- `dev-handoff.md`: development handoff history. The top 2026-04-25 update is readable and useful; older sections contain mojibake and should be treated as historical context only.
- `HANDOFF-FASTAPI.md`: FastAPI runtime handoff. Most operational structure is readable, but several product-flow names and old notes contain mojibake. The current canonical runtime contract is in `../PROJECT.md` and `../DEPLOYMENT.md`.

## Encoding Repair Decision

The corrupted historical text cannot be restored safely from the current repository contents. Instead of guessing the original wording, this archive keeps the original files unchanged and provides this clean index to mark their reliability.
