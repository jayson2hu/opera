# 修复提交独立最终验收 · 2026-09-12

## 结论

修复提交 **`f724cd70e57b5f6fe1abddd86a13c81d3d3b5a5a` 工程验收通过**。
提交后已从 Git 导出干净副本重新验证。本报告与结果 JSON 单独归档，不覆盖同时
写入仓库的其他复验报告；文档归档不改变被测生产代码。

本次没有推送远程或触发 GitHub Actions。提交使用命令级自动化身份
`Opera Acceptance Agent <opera-acceptance@localhost>`，没有修改全局 Git 配置。

正式发布仍待真实模型内容质量、原生 Safari/iOS 和实际中文输入法验收；本轮付费
调用为 0。工程通过不是公网无鉴权 SaaS 发布许可，也不表示所有软件没有未知漏洞。

精简证据见 [结果 JSON](acceptance/2026-09-12-final-results.json)，复跑步骤见
[验收说明](../tests/acceptance/README.md)。[上一轮报告](ACCEPTANCE-2026-09-12.md)
中的未提交状态、177 项后端测试和 8 项依赖告警已由此次结果更新。

## 修复与补强

- 上轮 10 类产品/部署问题均已复验：存储异常白屏、过期候选、保存失败保护、
  组合输入、复制反馈、深色对比度、非法 UTF-8、合法 SSE 数据行、空生成结果及默认网络暴露。
- 进一步拒绝空标签组、空白标签/组名，以及原创接口的空标签或空图片关键词；
  整稿和旧局部生成路径不能将这些结果标为完成，新增 7 项业务边界回归。
- 前端原 8 项 npm 告警全部关闭。没有跨直接依赖主版本，没有使用 `--force` 或 overrides。
- 后端增加生产/开发 SHA-256 锁文件及 5 项锁检查回归，验证 manifest、extras、
  传递依赖、安装版本、哈希和两锁一致性；生产 24 项、Linux 开发环境 33 项依赖通过检查。
- 测试依赖加入 httpx2，消除旧 TestClient 提醒；生产 provider 继续使用 httpx。
  依据：[Starlette 官方发布说明](https://www.starlette.dev/release-notes/#120-may-28-2026)。
- Nginx 从旧 1.27 分支升级至稳定版 1.30.4 并固定镜像 digest，增加前端健康检查。
  依据：[官方下载](https://nginx.org/en/download.html)、
  [安全公告](https://nginx.org/en/security_advisories.html)。当前配置未启用相关公告中的
  map/SSI/slice 等功能，不能把旧版本号直接当作已复现的可利用漏洞。
- 后端通过 `exec` 让 Uvicorn 成为 PID 1，实际停止耗时 0.57 秒、退出码 0。
- 浏览器验收筛选器无匹配或正则非法时返回非零，避免零用例假绿；非法正则在启动浏览器前拒绝。
- CI 补齐依赖审计、锁检查、Docker HTTP/浏览器验收和按 SHA 留存 14 天的测试证据。
  配置已本地核对，但没有将其写成远程 CI 已通过。

### 前端安全更新

| 依赖 | 旧锁版本 | 新锁版本 |
|---|---|---|
| @babel/core | 7.29.0 | 7.29.7 |
| @humanfs/node | 0.16.7 | 0.16.8 |
| vitest / @vitest/mocker | 4.1.10 | 4.1.11 |
| baseline-browser-mapping | 2.10.12 | 2.11.22 |
| brace-expansion | 5.0.8 | 5.0.9 |
| browserslist | 4.28.1 | 4.28.9 |
| nanoid | 3.3.16 | 3.3.19 |

原 npm 10.9.7 的自动修复遇到 optional-peer 解析异常，仅临时用 npm 12.0.2 生成锁；
随后用原 npm 10.9.7 成功执行 `npm ci`，包含开发依赖的审计结果为 0 告警。
逐项官方公告链接保存在结果 JSON，没有更新全局 npm。

## 提交后的实际验收

| 检查 | 结果 |
|---|---|
| 前端 Vitest | 136 / 136，20 个测试文件 |
| ESLint / TypeScript / Vite | 全通过 |
| 后端 pytest | 189 / 189：180 业务测试 + 5 锁检查 + 4 验收安全测试 |
| 锁一致性 / pip check / Ruff 0.16.7 | 全通过 |
| Docker 后端真实 HTTP | 67 / 67，75 次本地协议桩调用 |
| Nginx 真实 HTTP / SSE 代理 | 69 / 69，75 次本地协议桩调用 |
| Docker 前端 Chromium 交互 | 30 / 30 组 |
| 浏览器 → Nginx → FastAPI → 本地协议桩 | 三流程 3 / 3 |
| 三服务健康 / 仅本机绑定 / Nginx 配置 | 全通过 |
| 生产镜像 pip 下载缓存 | 0 项 |

浏览器覆盖 390 / 860 / 1440 / 1920、浅深色、稿件恢复/切换、候选确认/过期/放弃、
存储不可用/配额不足、多窗口冲突、版本恢复、删除失败、复制、JSON 备份和实际
1080×1440 PNG 下载。组合输入事件测试不替代真实操作系统输入法。

流式验收检查响应正文逐批到达，Nginx 下六组实际间隔约 143–150 毫秒，并非只断言
最终文本或响应头。覆盖 OpenAI/DeepSeek 兼容协议、Anthropic 协议、截断、上游错误、
模型允许列表及六个当前内容局部生成目标。

环境：Ubuntu 22.04.3、Node 22.22.2、Python 3.12.14、Docker Engine 29.1.3、
临时 Compose 5.5.1 / Buildx 0.37.0、Playwright 1.63.0 / Chromium 153.0.8010.12。
开发依赖经临时 uv 0.11.16 按 Python 3.12 目标哈希安装；生产 Dockerfile 用 pip
直接安装生产锁。没有把未执行的远程 CI 安装步骤冒充本地执行结果。

## 代码、产物与架构一致性

提交归档与 Docker 构建副本逐字节核对 **151 个文件，差异为 0**，源码指纹为
`ffd2182d4c5044cf3044554513374c5775a15087304d704c2fd10408ac5fe1ca`。
宿主构建采用与生产相同的空 `VITE_API_BASE_URL` 后，HTML、CSS、JS 与容器产物一致。
未设置与空字符串最初产生了等价的 4 字节 JS 差异，已核对并统一构建参数。

架构仍为 React → 同源 Nginx `/api` → FastAPI → provider adapter，保留三流程、
本地稿件和候选确认边界。没有引入账号、云稿件、自动发布、计费或新的应用框架。
此前 W0–W5 映射仍适用，本轮主要补齐 W5 的依赖、提交与可复跑验收证据。

## 清理与剩余项

专用容器、镜像、网络、卷、构建器和预览服务均已清理，四个测试端口已释放。
约 1 GiB 的临时依赖、浏览器、截图、样本下载、构建副本和两处新建仓库缓存已删除；
需要时可用验收脚本重新生成。清理明细见结果 JSON 的 `cleanup` 字段。

通用基础镜像的删除最初被安全检查暂缓；核对它们均为本轮新增且没有容器引用后，
精确 ID、非强制删除获准并完成。原有容器、前端 node_modules、后端虚拟环境和缓存
均保留。其他项目并行创建的容器、镜像、网络及共享工具状态没有清理；没有全局 prune。

唯一 pytest 提醒为 Starlette 1.6.0 引用 AnyIO 4.15.1 的弃用别名；没有屏蔽提醒、
打第三方补丁或为通过测试而降级，需后续跟进上游修复。

真实模型质量、原生设备/输入法和远程 CI 仍未验收。图片仍为临时资产，同稿多窗口
没有原子协作保证，JSON 备份没有通用导入界面；这些已声明的产品边界没有改变。
